# MakeCode

---

![MakeCode](pathname:///img/makecode.png)

For users who enjoy block-coding, MakeCode allows [micro:bit](../system/microbit) users to utilize hundreds of DUELink modules.

To connect DUELink modules, you need a micro:bit accessory that adds a JST connector, like the one we use for [Downlink](../interface/downlink). For example, [Sparkfun Qwiic micro:bit Breakout](https://www.sparkfun.com/sparkfun-qwiic-micro-bit-breakout.html) is a good option.

![micro:bit](pathname:///img/microbit-sparkfun-duelink.png)

This board connects the JST connector to the [I2C](../interface/i2c) bus, which is one of the supported [Interfaces](../interface/intro).

Lets get started! Create a MakeCode project like you always did on [makecode.microbit.org](https://makecode.microbit.org/).

![micro:bit MakeCode](pathname:///img/microbit-makecode.png)

This example blinks the status LED 50 times on any downlink module

```js
function SendCommand(str: string) {
  pins.i2cWriteBuffer(0x1a, Buffer.fromUTF8(str), false);
}
SendCommand("statled(200,200,50)");
```

In blocks, the code above looks like this:

![MakeCode Send Commands](pathname:///img/makecode-blocks.png)

:::tip
A DUELink extension will be provided in the future to make the use of MakeCode easier.
:::
